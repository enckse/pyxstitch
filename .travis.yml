language: python
python:
    - "3.6"

before_install:
    - sudo apt-get -qq update
    - sudo apt-get install -y golang gcc python3-pil python3-pygments

script:
    - make

notifications:
    email:
        on_success: change
        on_failure: change
